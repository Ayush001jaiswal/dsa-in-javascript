const A = 'abb';
let n = A.length;
let ans = 0;
for(let i =0; i<n; i++) {
    // for(let s=i-1; s>=0; s--) {
    //     if()
    // }
    // for() {

    // }
    let tempStr = leftStr + rightStr;
    if(tempStr.length > output.length) {
        output = tempStr;
    }
}
    console.log(output);